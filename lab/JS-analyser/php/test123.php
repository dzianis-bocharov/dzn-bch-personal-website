<?php

    each(scalesOptions, (axisOptions, axisID) => {

        axisOptions.id = axisID;
        
    });


    $round_brackets_indicator = $curly_braces_indicator + preg_match_all("/{/",$file_lines_array[$x]) - preg_match_all("/}/",$file_lines_array[$x]);

    $round_brackets_indicator++;

    if

    !


?>